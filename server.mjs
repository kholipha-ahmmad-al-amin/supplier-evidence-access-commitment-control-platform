import express from'express';import{join}from'node:path';import{JsonCommitmentStore}from'./store.mjs';import{CommitmentControlService}from'./domain.mjs';const store=new JsonCommitmentStore(process.env.DATA_FILE||join(process.cwd(),'data','commitments.json')),service=new CommitmentControlService(store.load(),state=>store.save(state)),app=express(),safe=fn=>(req,res)=>{try{fn(req,res)}catch(error){res.status(400).json({error:error.message})}};app.use(express.json());app.get('/health',(_,res)=>res.json({status:'ok',service:'supplier-evidence-access-commitment-control-platform'}));app.get('/commitments',(_,res)=>res.json(service.state.commitments));app.post('/commitments',safe((req,res)=>res.status(201).json(service.create(req.body))));app.post('/commitments/:id/review',safe((req,res)=>res.json(service.review(req.params.id,req.body.actor,req.body.note))));app.post('/commitments/:id/accept',safe((req,res)=>res.json(service.accept(req.params.id,req.body.actor))));app.post('/commitments/:id/confirm',safe((req,res)=>res.json(service.confirm(req.params.id,req.body.actor,req.body.confirmation))));app.post('/commitments/:id/verify',safe((req,res)=>res.json(service.verify(req.params.id,req.body.actor,req.body.evidence))));app.get('/commitments/:id/history',safe((req,res)=>res.json(service.history(req.params.id))));app.listen(process.env.PORT||58400,'0.0.0.0');
